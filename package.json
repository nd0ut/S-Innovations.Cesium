{
  "name": "cesium-typings-generator",
  "version": "1.0.0",
  "description": "",
  "main": "dist/cesium.d.ts",
  "scripts": {
    "mono": "cd src && ./build.sh && cd ..",
    "dist": "cd src && ./run.sh && cd ../ && grunt && cd dist",
    "build": "npm run mono && npm run dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "dts-generator": "^2.1.0",
    "grunt": "^0.4.5",
    "grunt-prepend": "^0.1.2",
    "typescript": "^2.5.0"
  }
}
